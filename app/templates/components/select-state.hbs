{{#ember-chosen id=(concat idPrefix '_state') name=(concat namePrefix '_state') class=(concat class ' form-select select-chosen')}}
  <option value="">Select</option>
  {{#each states as |state|}}
    {{#if (isEqualTo state.statekey selected)}}
      <option value="{{state.statekey}}" selected>{{state.statename}}</option>
    {{else}}
      <option value="{{state.statekey}}">{{state.statename}}</option>
    {{/if}}
  {{/each}}
{{/ember-chosen}}