/*jslint white:true, devel:true, es6:true, this:true, browser:true */
import Ember from 'ember';

export function lineBreaks(params/*, hash*/) {
  return params;
}

export default Ember.Helper.helper(lineBreaks);
